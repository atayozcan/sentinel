# Rust
/target/
/crates/*/target/
**/*.rs.bk
Cargo.lock.bak

# IDE / editor cruft
.idea/
.vscode/
.claude/
.qmlls.ini
*.swp
*.swo
*~
.DS_Store

# AUR makepkg artefacts (root-level only — do not match crates/*/src)
/pkg/
/src/
*.pkg.tar.zst
*.pkg.tar.xz
/packaging/arch/*.tar.gz
/packaging/arch/sentinel-*
# Re-include the install hook and the polkit admin rules — both are
# real source files the AUR PKGBUILDs read, not makepkg-generated output.
!/packaging/arch/sentinel-kde.install
!/packaging/arch/sentinel-*.rules

# Release artefacts
/dist/
*.tar.gz
*.tar.xz
*.sha256

# Debug
core
*.core
*.log

# Local task scratch
/TODO
/.notes/
