#!/bin/sh
# SPDX-FileCopyrightText: 2025 Atay Özcan <atay@oezcan.me>
# SPDX-License-Identifier: GPL-3.0-or-later

# postinst — runs after dpkg unpacks files.
# The polkit agent is shipped as an XDG autostart entry; no maintainer
# action needed here. The compositor will pick it up on next session
# start.
set -e
exit 0